language: node_js

node_js:
  - 9
cache:
  directories:
    - ~/.npm
    - ~/.cache

install:
  - yarn install

before_script:
  - yarn start -- --silent &

script:
  - yarn build
  - yarn run cypress:run
  - yarn test

after_success:
  - surge --project ./build --domain test-homework.surge.sh
